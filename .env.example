# =============================================================================
# AIOS Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# =============================================================================
# Environment Mode
# =============================================================================
# Options: development, staging, production
AIOS_ENV=development

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Provider Selection: "openai" or "anthropic"
LLM_PROVIDER=openai

# OpenAI API Key (required if using OpenAI)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# Anthropic API Key (required if using Claude)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# =============================================================================
# API Server Configuration
# =============================================================================
AIOS_DEBUG=false
AIOS_PORT=8000
AIOS_HOST=0.0.0.0

# =============================================================================
# Security Configuration
# =============================================================================
# CORS allowed origins (comma-separated)
# In production, specify exact domains: https://app.example.com,https://admin.example.com
AIOS_CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# API rate limit (requests per minute per key)
AIOS_RATE_LIMIT=100

# =============================================================================
# Supabase Configuration (required for Knowledge Layer)
# =============================================================================
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co

# Use anon key for client-facing requests (with RLS)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key for admin operations (bypasses RLS)
# SECURITY: Only use server-side, never expose to clients
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# Knowledge Layer API
# =============================================================================
KB_PORT=3001
KB_HOST=0.0.0.0

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Audit log destination (file path or "stdout")
AUDIT_LOG_PATH=./logs/audit.log

# =============================================================================
# Production Security Checklist
# =============================================================================
# Before deploying to production:
# [ ] Rotate all API keys
# [ ] Set AIOS_ENV=production
# [ ] Configure specific CORS origins (no wildcards)
# [ ] Enable HTTPS (use reverse proxy like nginx)
# [ ] Set up log aggregation (CloudWatch, Datadog, etc.)
# [ ] Configure Supabase RLS policies
# [ ] Set up API key rotation schedule
# [ ] Enable database backups
# [ ] Configure rate limiting per your needs
# [ ] Review and restrict API permissions
